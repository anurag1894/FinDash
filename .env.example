# Create .env.local from this file. Never expose these values in browser code.
KITE_API_KEY=replace_me
KITE_API_SECRET=replace_me
KITE_REDIRECT_URL=http://localhost:4173/api/auth/kite/callback
PORT=4173

# Password protection (required in production)
APP_PASSWORD=choose_a_strong_password

# Session signing key (auto-generated if not set, but set for persistence across deploys)
SESSION_SECRET=replace_with_random_64_char_hex
